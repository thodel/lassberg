[project]
name = "vlm-htr-training"
version = "0.1.0"
description = "QLoRA fine-tuning pipeline for historical HTR using Qwen3-VL"
requires-python = ">=3.10"
dependencies = [
    "transformers>=4.51.0",
    "datasets>=3.0.0",
    "peft>=0.14.0",
    "trl>=0.13.0",
    "bitsandbytes>=0.45.0",
    "accelerate>=1.3.0",
    "deepspeed>=0.16.0",
    "Pillow>=10.0.0",
    "evaluate",
    "jiwer",
    "wandb",
]

